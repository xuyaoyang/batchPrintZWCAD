# 修订2：通用图框可切换相邻 A 系列图幅

修复：比例库匹配成功后过早返回，仅列出 1:100、1:50 等内置比例对应的图幅，遗漏 A4→A3、A3→A2 所需的任意比例。

现在保留原先识别出的默认图幅和候选顺序，再补齐缺少的 A0～A4 标准图幅。比例按实际短边与目标纸张短边换算。例如 A4@1:100→A3 显示 1:70.707，A3@1:100→A2 显示 1:70.714；毫米规格有取整，两个数值略有差异是正常的。加长框不强行转成标准长宽比。

保留图片签章版1的全部功能和限制：签章只用于选图框块批打，通用矩形批打不加签章。

加载：保存工作并重启 CAD；NETLOAD 加载本修订2目录里的 BatchPlotter.dll，保留同目录依赖与 Plotters。若曾设置其他版本自动加载，将其改指向本目录。通用批打窗口标题应显示“修订2”；重新扫描或重新识别后使用新的候选列表。

验证：tests/PaperSwitch 最小测试在修复前 13 项失败，修复后 25 项全通过（横/竖版 A4→A3、A3→A2、换算比例、全部标准图幅、默认项、布局1:1、加长图和非图框过滤）。独立中望机械版2026运行原上游16项检查通过，并完成图片签章的PDF/PNG/JPG及合并、旋转、留白回归。没有操作用户的业务 DWG。
